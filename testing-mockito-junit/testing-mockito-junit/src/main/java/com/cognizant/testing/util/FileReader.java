package com.cognizant.testing.util;

/**
 * FileReader – plain interface used in Advanced Mockito Exercise 3 (Mocking File I/O).
 * Named distinctly from java.io.FileReader to avoid confusion/clashes.
 */
public interface FileReader {
    String read();
}
